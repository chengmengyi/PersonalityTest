package com.personalitytest.individuality.admob

class AdmobType {
    companion object{
        const val OPEN="per_open"
        const val HOME="per_home"
        const val ALL_TYPE="per_all_type"
        const val ANSWER_RESULT="per_answer_result"
        const val QUESTION="per_question"
        const val QUESTION_FINISH="per_question_finish"
    }
}